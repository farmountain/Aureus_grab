{
  "name": "aureus-agentic-os",
  "version": "0.1.0",
  "description": "Production-grade operating system for AI agents",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:ordered": "npm run build --workspace=@aureus/observability && npm run build --workspace=@aureus/world-model && npm run build --workspace=@aureus/policy && npm run build --workspace=@aureus/crv && npm run build --workspace=@aureus/tools && npm run build --workspace=@aureus/hypothesis && npm run build --workspace=@aureus/kernel && npm run build --workspace=@aureus/memory-hipcortex && npm run build --workspace=@aureus/perception && npm run build --workspace=@aureus/reflexion && npm run build --workspace=@aureus/benchright && npm run build --workspace=@aureus/evaluation-harness && npm run build --workspace=@aureus/robotics && npm run build --workspace=@aureus/sdk",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "deploy:console": "npm run build:ordered && npm run build --workspace=@aureus/console && cd apps/console && npm start",
    "deploy:packages": "npm run build:ordered && npm pack --workspaces",
    "backup:state": "bash scripts/backup-state.sh",
    "backup:events": "bash scripts/backup-events.sh",
    "restore:state": "bash scripts/restore-state.sh",
    "restore:events": "bash scripts/restore-events.sh"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.1",
    "pg": "^8.16.3",
    "zod": "^4.3.5"
  }
}
