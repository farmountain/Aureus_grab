{
  "name": "@aureus/memory-hipcortex",
  "version": "0.1.0",
  "description": "Temporal index, snapshots, audit log with rollback",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && node -e \"require('fs').copyFileSync('src/db-schema.sql', 'dist/db-schema.sql')\"",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@types/pg": "^8.16.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
